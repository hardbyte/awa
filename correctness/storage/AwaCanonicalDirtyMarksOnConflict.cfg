\* v006 semantics: INSERT ... ON CONFLICT DO NOTHING against a keyed dirty
\* row. Expected counterexample: two transactions that share only a queue
\* name close a waits-for cycle through the uncommitted mark row.
CONSTANTS
  Txs <- AppTxs
  WaitFree = FALSE

SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoDeadlock

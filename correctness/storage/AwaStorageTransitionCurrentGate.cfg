\* Expected witness for the current SQL-level gate if it only checks
\* `runtime_instances.storage_capability = 'queue_storage'`.
\*
\* An auto queue-storage-capable runtime started before mixed transition reports
\* `queue_storage` while prepared, so it satisfies the current SQL gate. After
\* mixed transition it becomes canonical-drain-only because its effective
\* storage was resolved at startup. If no explicit queue-storage target is live,
\* the system is in mixed_transition with no queue executor.
CONSTANTS
  MaxCanonicalBacklog = 1
  MaxQueueRows = 1
  RequireQueueExecutorOnEnter = FALSE

SPECIFICATION Spec

INVARIANTS
  TypeOK
  PreparedRequiresEngine
  ActiveImpliesQueueStorageCurrent
  CanonicalAndPreparedRouteCanonical
  MixedAndActiveRouteQueueStorage
  NoMixedWithCanonicalOnlyRuntime
  FinalizeOnlyAfterDrain
  FinalizeOnlyAfterNoDrainRuntimes
  MixedHasQueueExecutor
